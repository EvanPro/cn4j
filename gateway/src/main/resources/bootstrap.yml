spring:
  cloud:
    kubernetes:
      config:
        sources:
          - name: gateway
            namespace: wm-test
      reload:
        enabled: true
        mode: polling
        monitoring-secrets: true
