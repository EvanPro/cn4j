spring:
  cloud:
    kubernetes:
      config:
        sources:
          - name: system
            namespace: wm-test
      reload:
        enabled: true
        mode: polling
        monitoring-secrets: true
